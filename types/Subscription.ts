export interface ISubscription {
    _id?: string,
    name: string,
    price: number,
    currency: string,
    frequency: string,
    category: string,
    paymentMethod: string,
    startDate: string,
    status: string
}

